# 2026-06-24

## ContentLibraryCreatePage 修复
- 所属业务/场景/车系/车型：从 checkbox 多选改为 radio 单选
- 是否配置雷达：从 toggle 开关改为"是/否" radio 单选，选"是"时展开雷达选项卡片
- 清理未使用 import：ChevronUp、ImageIcon、Trash2、coverImage 等

## ContentLibraryAdminPage 修复
- 左侧目录自动收起：点击其他一级菜单时，当前展开的菜单自动收起
- 新建文件夹按钮：改为加号图标按钮，与搜索框并排，更简洁
- 筛选栏布局：从 4 列 grid 改为 flex 分行布局，每行 4 个筛选项，标签对齐，宽度统一，按钮独立一行右对齐
- ContentLibraryCreatePage 同步修复菜单收起逻辑
- 左侧深色菜单栏：改为 hover 自动展开/收起（mouseenter 展开，mouseleave 收起），默认收起状态，移除底部手动折叠按钮
- 新建素材弹窗"封面图"改为多图上传：支持 FileReader 本地上传，最多 9 张，显示已上传数量提示，超出时 toast 警告，每张图片可 hover 删除，全局 toast 组件（顶部居中，绿色勾+消息，2 秒自动消失，带进入/离开动画）
- ContentLibraryCreatePage 同步修复：左侧菜单 hover 自动收起、上传图片改为多图（最多9张）+ toast 提示、标签从"封面图"改为"上传图片"
