# 2026-06-23

## 新增内容库后台管理页面
- 创建了 `src/features/content-library-admin/ContentLibraryAdminPage.vue`
- 三栏布局：左侧深色菜单栏（可折叠） + 文件夹树 + 右侧主内容区
- 左侧菜单"内容中心"二级菜单：内容库（高亮）、会话存档（按截图修正，去掉素材管理/标签管理）
- **布局调整**：文件夹树在筛选栏下方，与素材列表左右平齐（不是三栏独立布局）
- 筛选栏：大区/经销商/所属业务/素材类型/标题/所属场景/发布状态/更新时间(起止日期)，含查询/重置按钮
- 文件夹树：全部(156)、2024车型(45)、售后活动素材(32)、新Q8素材(28)，含搜索框和新建文件夹
- 表格列（按截图100%还原）：checkbox、来源、类型、图片、标题、所属场景、所属车系、所属车型、素材状态、所属业务、运营人员、更新时间、操作(编辑/发布/下架/推广)
- 素材数据：含"企微创建"和"内容中台"两种来源，状态分已发布/已下架
- 顶部有「手机端预览」按钮，跳转到 `/content-library`
- 新建/编辑弹窗：素材名称、素材类型、所属业务、所属场景、经销商、封面图、描述、内容
- 路由：`/content-library-admin`
- Sprint 9 需求"内容库瀑布流改造2.0"的 link 指向 `/content-library-admin`
- 手机端内容库（`/content-library`）保持独立，与后台不混淆

## 新增新建素材页面
- 创建了 `src/features/content-library-admin/ContentLibraryCreatePage.vue`
- 路由：`/content-library-admin/create`
- 列表页「添加」按钮跳转到此页面
- 表单字段（按截图100%还原）：
  - 内容类型：文本/图片/文章(选中)/视频/链接/文件/小程序/语音/海报（radio）
  - 标题：输入框 + 0/30字数
  - 封面图：支持多张上传（拖拽排序），第一张为封面（红色边框+封面标签），其余为banner展示图，每张可独立删除
  - 描述：输入框 + 0/30字数
  - 文章正文：富文本编辑器（工具栏：字体、B/I/U/S、引用、代码、标题、对齐、列表、上下标、分割线、字号、颜色、链接、图片）
  - 有效期：永久有效/自定义（radio）
  - 所属业务：品质车型/品牌种草/售后服务（radio单选）
  - 所属场景：车型参数/外观/内饰/核心卖点/友商对比/营销海报/购车权益（radio单选）
  - 所属车系：A系列/Q系列/纯电/旅行车/高性能（radio单选）
  - 所属车型：A5L/A6L/A4L/A8L/A3/A5/A7 Sportback/全新A6L（radio单选）
  - 使用范围：全国 + 添加部门按钮
  - 是否配置雷达：是/否（radio单选），选"是"时展开四个选项卡片(行为通知/动态通知/客户标签/数据雷达)
  - 底部：返回/保存/发布按钮
- 布局优化：统一表单行高度(h-9)、标签宽度(w-24)、间距(mb-5)，工具栏按钮统一定宽(w-7 h-6)，radio尺寸统一(3.5)，确保单行不换行
- 修复：移除 PrototypeCodeLocation 组件（避免 getBoundingClientRect 报错），expandedMenu 保持"内容中心"展开
- 修复(24日)：所属业务/场景/车系/车型从checkbox多选改为radio单选，是否配置雷达从toggle开关改为是/否radio单选
